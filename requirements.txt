# Python deps for the delivery-ops MCP server and the OCR test harness.
# Unpinned per repo convention; pin only if a compatibility issue forces it.
# Telegram + the agent loop are handled by OpenClaw (Node/grammY), not here.

mcp                 # Model Context Protocol Python SDK (stdio + streamable-http)
uvicorn             # ASGI server backing the streamable-http transport (sidecar mode)
openai              # Azure OpenAI via its OpenAI-compatible v1 endpoint (vision input)
aiosqlite           # async SQLite for the cost ledger
matplotlib          # budget-vs-actual chart rendering (PNG)
python-dotenv       # load .env in local dev and the OCR harness

# Tests (python -m pytest tests/)
pytest              # test runner
pytest-asyncio      # async test support for the aiosqlite ledger
